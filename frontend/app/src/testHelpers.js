export const renderWithProviders = (ui, { providerProps, ...renderOptions } = {}) => {
    // add implementation for rendering with providers
};

export const mockAuthUsers = [{
    // add implementation for mock authentication users
}];